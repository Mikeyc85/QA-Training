alpha=[0]*26
msg=input("Enter any message")
for ch in msg:
    if alpha[ord(ch) in msg]>=65 and <=90:
            alpha[ord(ch)-65]+=1
    
        i=0
        while i<len(alpha):
            if alpha[i]!=0:
                print(chr(i+65),";",alpha[i])
        i+=1
    elif: 
        alpha[ord(ch) in msg] >=97 and <=122
            alpha[ord(ch)-97]+=1
        i=0
        while i<len(alpha):
            if alpha[i]!=0:
                print(chr(i+65),";",alpha[i])
        i+=1